/* eslint-disable @typescript-eslint/no-unused-vars */

import { IEditComponentProps } from "@/types/global/global.types";


export default function EditUser({ setEditModalOpen, refetch }: IEditComponentProps) {
    // export default function EditUser({ setEditModalOpen, refetch }: IUserEditProps) {
    return (
        <div>EditUser</div>
    )
}
