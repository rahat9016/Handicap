import { Metadata } from "next";

export const metadata: Metadata = {
    title: "Handicap International | admin",
};

export default function Page() {
    // const DynamicAdmin = dynamic(
    //     () => import("@/components/dashboard/admin/Admin"),
    //     {
    //         loading: () => <Loader />
    //     }
    // );
    return (
        <div></div>
    )
}
